import type * as Stitches from "@stitches/core";

type BaseLetterSpacingsProps = {
  letterSpacing?: Stitches.PropertyValue<"letterSpacing">;
};

export type LetterSpacingProps = BaseLetterSpacingsProps;
