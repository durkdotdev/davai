import type * as Stitches from "@stitches/core";

type BaseShadowsProps = {
  boxShadow?: Stitches.PropertyValue<"boxShadow">;
  textShadow?: Stitches.PropertyValue<"textShadow">;
};

export type ShadowsProps = BaseShadowsProps;
